DEFINE INPUT PARAMETER intKey AS INTEGER.
DEFINE INPUT PARAMETER inpChar1 AS CHARACTER.

DEFINE OUTPUT PARAMETER check-parent AS LOGICAL INITIAL NO.

FIND FIRST queasy WHERE queasy.KEY = intKey
    AND queasy.char1 = inpChar1 NO-LOCK NO-ERROR.
IF NOT AVAILABLE queasy THEN check-parent = YES.
