DEFINE INPUT PARAMETER case-type AS INTEGER.
DEFINE INPUT PARAMETER q-recid   AS INTEGER.

DEFINE BUFFER qsy FOR queasy.
IF case-type = 1 THEN
DO:
    FIND FIRST qsy WHERE RECID(qsy) = q-recid EXCLUSIVE-LOCK.
    DELETE qsy.
    RELEASE qsy.
END.
